const readline = require('readline-sync');

const jogo = ['pedra', 'papel', 'tesoura']

let opcaoJogador
let placar = []


function menu(){

}

function jogo(){
    let opcaoAleatoria = jogo[Math.floor(Math.random() * jogo.length)]

    console.log('Você escolhe pedra, papel ou tesoura?');
    
    opcaoJogador = readline('')
    
    if(!jogo.includes(opcaoJogador)){
        console.log('Opção inválida!')

    }
 
}